{
  "name": "hexos-engine",
  "version": "0.1.0",
  "description": "HEXOS TCG game engine",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "tsx --test tests/**/*.test.ts",
    "typecheck": "tsc --noEmit",
    "dev": "tsx src/cli/index.ts"
  },
  "devDependencies": {
    "typescript": ">=5.0.0",
    "tsx": ">=4.0.0"
  }
}
